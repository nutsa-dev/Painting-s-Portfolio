# Workspace Rules

- Whenever code changes are made during a task, automatically create clean, descriptive Git commits and push them to the remote GitHub repository.
- Use `& "C:\Program Files\Git\cmd\git.exe"` if `git` is not in the environment PATH.
